import { FC } from 'react';
import Layout from '../../components/Layout/Layout';

const Blog: FC = () => {
    return (
        <Layout>
            <div>Blog</div>
        </Layout>
    );
};

export default Blog;